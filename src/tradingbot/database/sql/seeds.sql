/*
=============================================================
INITIAL DATA SEEDING (Optional)
=============================================================
*/

-- Insert default admin user (password should be hashed in real implementation)
-- INSERT INTO users (id, email, username, first_name, last_name, hashed_password, is_admin) 
-- VALUES (UUID(), 'admin@cryptobot.local', 'admin', 'System', 'Administrator', '$2b$12$...', TRUE);

-- Insert sample strategy types for reference
-- This would typically be handled by the application, not the database

